apiVersion: v2
name: home-cloud-secrets
description: >
  Dynamic secrets with HashiCorp Vault (counterpart of AWS Secrets Manager / KMS).
  Wires the secrets Namespace, public IngressRouting and connector values;
  Vault itself is installed from the hashicorp chart (see NOTES).
type: application
version: 0.1.0
appVersion: "0.1.0"
